services:
  node-example-dev:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
      args:
        DEVBASE_IMAGE: ${DEVBASE_IMAGE:-ghcr.io/komadori-dev/devbase:latest}
    volumes:
      - .:/workspace/node-example
    command: sleep infinity
